name: nomadics2
run-name: Testing the App
on: [push]
jobs:
  test-output:
    runs-on: ubuntu-latest
    steps:
    - name: test output
      run: echo "This is my first test"
  connect-remote:
    runs-on: ubuntu-latest
    steps:
    - uses: mr-smithers-excellent/docker-build-push@v6
    - name: Build & push Docker image
      with:
        image: kwafatadns/dockerhub
        registry: docker.io
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

