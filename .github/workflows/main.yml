name: nomadics
run-name: Setting the code for the DB
on: [push]
jobs:
  test-output:
    runs-on: ubuntu-latest
    steps:
    - name: test output
      run: echo "This is my first test"
connect-db:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v3
  - name: show server_version();
    run: psql -h ${{ secrets.DBHOSTNAME}} -u${{secret.DB_USER}} --p${{secret.DB_PASSWORD}} --port 5432
